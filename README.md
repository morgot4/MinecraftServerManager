# 🎮 Minecraft Server Manager

Telegram-бот для полного управления Minecraft серверами. Позволяет запускать, останавливать, создавать сервера, управлять модами, игроками и бэкапами — всё через удобный интерфейс в Telegram.

## ✨ Возможности

### Управление сервером
- 🚀 Запуск/остановка/перезапуск сервера
- 📊 Мониторинг статуса в реальном времени
- 👥 Список онлайн игроков
- 💬 Отправка сообщений в чат сервера
- 🖥️ Выполнение команд в консоли

### Управление игроками
- ✅ Whitelist — добавление/удаление игроков
- 👑 Операторы — назначение прав
- 👢 Кик игроков

### Бэкапы
- 📦 Ручные бэкапы по команде
- ⏰ Автоматические бэкапы каждые 3 часа
- 🔄 Ротация (хранение 2 последних)
- ♻️ Восстановление из бэкапа

### Моды (Forge)
- 🔍 Поиск модов на Modrinth
- 📥 Установка модов одной командой
- 🗑️ Удаление модов
- 📋 Список установленных модов

### Мультисервер
- 📁 Создание нескольких серверов
- 🔀 Переключение между серверами
- ⚙️ Разные ядра: Vanilla, Forge

### Безопасность
- 🔐 Три уровня доступа: Admin, Moderator, Player
- 👤 Управление ролями пользователей

### Автоматизация
- 💤 Авто-выключение при отсутствии игроков
- 📬 Уведомления админу о событиях
- 🌍 Локализация: Русский и English

## 📋 Требования

- **Python 3.11+**
- **Java 17+** (для Minecraft 1.18+) или **Java 21** (для Minecraft 1.21+)
- **Ubuntu/Linux** сервер (рекомендуется 4+ ядер, 8+ GB RAM)
- Telegram Bot Token (от [@BotFather](https://t.me/BotFather))

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/MinecraftServerManager.git
cd MinecraftServerManager
```

### 2. Установка Poetry (если не установлен)

```bash
curl -sSL https://install.python-poetry.org | python3 -
```

### 3. Установка зависимостей

```bash
poetry install
```

### 4. Настройка конфигурации

```bash
# Копируем пример конфигурации
cp config.example.yaml config.yaml

# Редактируем конфигурацию
nano config.yaml
```

### 5. Настройка токена бота

Вариант 1 — через `.env` файл:
```bash
cp .env.example .env
echo "BOT_TOKEN=your_bot_token_here" > .env
```

Вариант 2 — в `config.yaml`:
```yaml
telegram:
  bot_token: "your_bot_token_here"
  admin_ids:
    - 123456789  # Ваш Telegram ID
```

> 💡 Узнать свой Telegram ID можно у бота [@userinfobot](https://t.me/userinfobot)

### 6. Запуск

```bash
poetry run python -m src.main
```

Или через Poetry script:
```bash
poetry run mcmanager
```

## ⚙️ Конфигурация

### config.yaml

```yaml
# Telegram настройки
telegram:
  bot_token: "${BOT_TOKEN}"  # Или прямо токен
  admin_ids:
    - 123456789              # Telegram ID администраторов

# Настройки по умолчанию для серверов
defaults:
  ram_min: "4G"              # Минимальная RAM
  ram_max: "10G"             # Максимальная RAM
  port: 25565                # Порт сервера
  java_path: "java"          # Путь к Java

# Авто-выключение
auto_shutdown:
  enabled: true
  empty_minutes: 30          # Минут без игроков до выключения

# Бэкапы
backups:
  auto_enabled: true
  interval_hours: 3          # Интервал автобэкапов
  keep_count: 2              # Сколько хранить
  backup_on_stop: true       # Бэкап перед остановкой

# Уведомления
notifications:
  server_start: true
  server_stop: true
  player_join: true
  player_leave: true
  crash: true

# Пути
paths:
  servers_dir: "./servers"
  backups_dir: "./backups"
  database: "./data/manager.db"

# Язык по умолчанию
language: "ru"
```

## 📱 Команды бота

### Для всех пользователей
| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/help` | Список команд |
| `/status` | Статус сервера |
| `/list` | Игроки онлайн |
| `/lang` | Сменить язык |

### Для модераторов
| Команда | Описание |
|---------|----------|
| `/on` | Запустить сервер |
| `/off` | Остановить сервер |
| `/restart` | Перезапустить |
| `/say <текст>` | Написать в чат |
| `/kick <ник>` | Кикнуть игрока |
| `/whitelist add/remove <ник>` | Whitelist |
| `/backup` | Создать бэкап |
| `/console <команда>` | Команда в консоль |

### Для администраторов
| Команда | Описание |
|---------|----------|
| `/servers` | Список серверов |
| `/switch <имя>` | Сменить сервер |
| `/create` | Создать сервер |
| `/delete <имя>` | Удалить сервер |
| `/op add/remove <ник>` | Операторы |
| `/settings` | Настройки сервера |
| `/set <ключ> <значение>` | Изменить настройку |
| `/version <версия>` | Сменить версию |
| `/mods` | Список модов |
| `/addmod <название>` | Установить мод |
| `/removemod <название>` | Удалить мод |
| `/backups` | Список бэкапов |
| `/restore <id>` | Восстановить бэкап |
| `/role <user_id> <роль>` | Назначить роль |

## 🏗️ Структура проекта

```
MinecraftServerManager/
├── src/
│   ├── main.py              # Точка входа
│   ├── core/                # Ядро управления
│   │   ├── server_manager.py
│   │   ├── process_handler.py
│   │   ├── log_watcher.py
│   │   ├── rcon.py
│   │   ├── backup_manager.py
│   │   └── auto_shutdown.py
│   ├── minecraft/           # MC-специфичное
│   │   ├── server_properties.py
│   │   └── whitelist.py
│   ├── engines/             # Ядра серверов
│   │   ├── vanilla.py
│   │   └── forge.py
│   ├── mods/                # Управление модами
│   │   ├── modrinth_api.py
│   │   └── mod_manager.py
│   ├── bot/                 # Telegram бот
│   │   ├── bot.py
│   │   ├── handlers/
│   │   ├── keyboards.py
│   │   └── notifications.py
│   ├── storage/             # База данных
│   │   ├── database.py
│   │   └── models.py
│   ├── i18n/                # Локализация
│   │   └── locales/
│   └── utils/               # Утилиты
├── servers/                 # Папки серверов
├── backups/                 # Бэкапы
├── data/                    # SQLite база
├── config.yaml              # Конфигурация
└── pyproject.toml           # Poetry
```

## 🐳 Docker (опционально)

```dockerfile
FROM python:3.11-slim

# Install Java
RUN apt-get update && apt-get install -y openjdk-21-jre-headless && rm -rf /var/lib/apt/lists/*

# Install poetry
RUN pip install poetry

WORKDIR /app
COPY . .

RUN poetry install --no-dev

CMD ["poetry", "run", "mcmanager"]
```

## 🔧 Запуск как systemd сервис

```ini
# /etc/systemd/system/mcmanager.service
[Unit]
Description=Minecraft Server Manager
After=network.target

[Service]
Type=simple
User=minecraft
WorkingDirectory=/home/minecraft/MinecraftServerManager
Environment=BOT_TOKEN=your_token_here
ExecStart=/home/minecraft/.local/bin/poetry run mcmanager
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable mcmanager
sudo systemctl start mcmanager
```

